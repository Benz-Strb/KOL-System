VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
# Leave empty for local dev (uses Vite's /api proxy, see vite.config.ts).
# Set to the deployed server Worker URL (e.g. https://kol-system-server.<account>.workers.dev)
# once client/server are deployed on separate origins.
VITE_API_BASE_URL=""
